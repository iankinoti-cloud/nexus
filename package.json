{
  "name": "nexus",
  "private": true,
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "slides": "npm --prefix slides run dev",
    "install:all": "npm --prefix app install && npm --prefix slides install"
  }
}
